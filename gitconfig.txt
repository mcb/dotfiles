[user]
	name = ${name}
	email = ${id}+${username}@users.noreply.github.com
[alias]
	lg = log --simplify-merges --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  c = commit
  st = status
  co = checkout
  cp = cherry-pick
  br = branch
  changes = diff --name-status -r
  diffstat = diff --stat -r
[core]
  excludesfile = ${HOME}/.gitignore
  editor = vim
  autocrlf = input
  whitespace = -trailing-space, -space-before-tab
[init]
  defaultBranch = main
[mergetool]
  keepBackup = false
  prompt = false
[apply]
  whitespace = nowarn
[diff]
  tool = diff
[merge]
  tool = diff
[push]
  default = simple
[pull]
  rebase = false
